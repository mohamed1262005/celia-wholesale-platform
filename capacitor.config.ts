import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.celia.wholesale',
  appName: 'Celia Premium Sweets',
  webDir: 'dist'
};

export default config;
